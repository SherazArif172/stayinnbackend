# Database
MONGODB_URI=mongodb://localhost:27017/stayinn

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Nodemailer (SMTP) - required for verification & password reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=your-email@gmail.com
# Frontend URL for verification & password-reset links (local dev vs production)
FRONTEND_URL=http://localhost:3000
# Production example: FRONTEND_URL=https://stayinn-hostels-launch.vercel.app
